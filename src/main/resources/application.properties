#不支持http和https同时启用的情况

#系统属性、环境变量、命令参数信息
#端口
server.port=1213
	##server.port=0 自动获取可用端口
	#地址
server.address=192.168.30.31

#生产环境  有生产环境（production),开发环境(development）,测试环境(test）
#spring.profiles.active=test 
 
#---------------MS SQL  Start---------------
#初始连接数量
spring.datasource.initial-size=0
#最大连接数量
spring.datasource.max-active=20
#最大闲置连接数
spring.datasource.max-idle=20
#最小闲置连接数
spring.datasource.min-idle=1
#最大等待时间
spring.datasource.tomcat.max-wait=5000

#多数据源配置
#===================================================正式数据库===================================================
spring.datasource.url=jdbc:sqlserver://192.168.0.91;instanceName=MSSQLSERVER;DatabaseName=SaaS;integratedSecurity=false
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.username=dev
spring.datasource.password=dev
        
### MY-SQL 配置
##spring.datasource.driver-class-name=com.mysql.jdbc.Driver
##spring.datasource.url=jdbc:mysql://localhost:3306/company?allowMultiQueries=true
##spring.datasource.username=root
##spring.datasource.password=xxx

#---------------DataSource Config End---------------


#--------------- redis 配置  start ---------------
 
##host和port默认值取值localhost，6379
#spring.redis.host=localhost
#spring.redis.port=6379
## 或 spring.redis.url= localhost:6379 


#链接池配置，下面是默认值

#spring.pool.max-idle=8

#spring.pool.min-idel=0 
#spring.pool.max-wait=-1 //-1禁用该属性，等待单位milliseconds 
#spring.pool.max-active=8

#密码，无密码时不需要
#spring.redis.password=
#spring.redis.ssl=true/false

#--------------- redis 配置  end ---------------

#如果不适用xml映射文件配置，下面的不需要，直接使用注解可以实现
#放在resources/mybatis/mybatis-config.xml中 
mybatis.config-location=classpath:mybatis/mybatis-config.xml 
#######mybatis.mapper-locaitons=classpath:mybatis/mappings/*.xml


#DEBUG模式 打印mybatiesSql语句
logging.level.com.example.mapper=DEBUG

#单个文件上传限制
spring.http.multipart.maxFileSize=10Mb
#单次文件上传限制
spring.http.multipart.maxRequestSize=100Mb


